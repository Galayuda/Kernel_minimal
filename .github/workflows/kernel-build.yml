# ============================================================================
# GitHub Actions Workflow для сборки МИНИМАЛЬНОГО ядра Linux 6.19.10
# Репозиторий: Kernel_minimal
# Конфигурация: minimal_config (1714 строк, -62% от оригинала)
# ============================================================================

name: Build Linux Kernel 6.19.10-minimal

# Триггеры запуска
on:
  # Запуск вручную через вкладку Actions
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Версия ядра'
        required: false
        default: '6.19.10'

      config_type:
        description: 'Тип конфигурации (defconfig или custom)'
        required: false
        default: 'custom'
        type: choice
        options:
          - custom
          - defconfig

# Основная задача
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ============================================================================
      # ШАГ 1: Checkout репозитория
      # ============================================================================
      - name: Checkout repository
        uses: actions/checkout@v4

      # ============================================================================
      # ШАГ 2: Установка зависимостей
      # ============================================================================
      - name: Install build dependencies
        run: |
          echo "🔧 Установка зависимостей для сборки ядра..."
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libncurses-dev \
            bison \
            flex \
            libssl-dev \
            libelf-dev \
            bc \
            cpio \
            dwarves \
            wget \
            git \
            kmod
          echo "✅ Зависимости установлены"

      # ============================================================================
      # ШАГ 3: Загрузка исходников ядра
      # ============================================================================
      - name: Download kernel source
        run: |
          echo "📥 Загрузка исходников ядра..."
          KERNEL_VERSION="${{ github.event.inputs.kernel_version || '6.19.10' }}"
          wget -q https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-${KERNEL_VERSION}.tar.xz
          tar -xf linux-${KERNEL_VERSION}.tar.xz
          mv linux-${KERNEL_VERSION} linux-build
          echo "✅ Исходники загружены: linux-${KERNEL_VERSION}"
          ls -lh

      # ============================================================================
      # ШАГ 4: Копирование конфигурации + сохранение для артефакта
      # ============================================================================
      - name: Copy kernel config
        id: copy_config
        run: |
          echo "📋 Копирование конфигурации..."
          CONFIG_TYPE="${{ github.event.inputs.config_type || 'custom' }}"

          # Переходим в папку с исходниками
          cd linux-build

          if [ "$CONFIG_TYPE" = "custom" ]; then
            # Используем наш minimal_config из репозитория
            if [ -f "$GITHUB_WORKSPACE/minimal_config" ]; then
              cp "$GITHUB_WORKSPACE/minimal_config" .config
              echo "✅ Использована minimal_config из репозитория (1714 строк)"
            else
              echo "⚠️ minimal_config не найден, создаём defconfig"
              make defconfig
            fi
          else
            # Создаём дефолтную конфигурацию
            make defconfig
            echo "✅ Создана defconfig"
          fi

          # 🔥 КРИТИЧНО: Сохраняем копию .config СРАЗУ (до сборки)
          cp .config "$GITHUB_WORKSPACE/minimal_config"
          echo "✅ .config сохранена в \$GITHUB_WORKSPACE/minimal_config"

          # Показываем ключевые опции
          echo ""
          echo "📊 Ключевые опции конфигурации:"
          grep -E "^CONFIG_(IWLWIFI|SND_HDA|DRM_I915|PREEMPT|LOCALVERSION)=" .config | head -10

      # ============================================================================
      # ШАГ 5: olddefconfig — разрешение зависимостей + лог предупреждений
      # ============================================================================
      - name: Resolve config dependencies
        run: |
          echo "🔧 Разрешение зависимостей конфигурации..."
          cd linux-build

          # olddefconfig автоматически разрешает зависимости и отключает невозможное
          make olddefconfig 2>&1 | tee /tmp/olddefconfig.log

          # Показываем что изменилось
          echo ""
          echo "📊 Ключевые опции ПОСЛЕ olddefconfig:"
          grep -E "^CONFIG_(IWLWIFI|IWLMVM|CFG80211|MAC80211|TRANSPARENT_HUGEPAGE|LOCALVERSION)=" .config

          # Сохраняем финальный конфиг для анализа
          cp .config "$GITHUB_WORKSPACE/minimal_config_final"

          echo "✅ Конфигурация разрешена"

      # ============================================================================
      # ШАГ 6: Подготовка к сборке
      # ============================================================================
      - name: Prepare kernel build
        run: |
          echo "🔧 Подготовка к сборке..."
          cd linux-build

          # Генерация заголовков
          make prepare 2>&1 | tee /tmp/prepare.log
          make scripts 2>&1 | tee /tmp/scripts.log

          echo "✅ Подготовка завершена"

      # ============================================================================
      # ШАГ 7: Сборка ядра + ПОЛНЫЙ ЛОГ modules
      # ============================================================================
      - name: Build kernel
        run: |
          echo "🚀 Начало сборки ядра..."
          cd linux-build

          # Получаем количество доступных CPU
          CPU_COUNT=$(nproc)
          echo "📊 Доступно CPU: $CPU_COUNT"

          # Замер времени
          START_TIME=$(date +%s)

          # Сборка bzImage — обычный вывод
          echo "📦 Сборка bzImage..."
          make -j$CPU_COUNT bzImage

          # Сборка модулей — ПОЛНЫЙ ЛОГ (не tail!)
          echo ""
          echo "📦 Сборка модулей (полный лог)..."
          make -j$CPU_COUNT modules 2>&1 | tee /tmp/modules_build.log

          # Время сборки
          END_TIME=$(date +%s)
          ELAPSED=$((END_TIME - START_TIME))
          MINUTES=$((ELAPSED / 60))
          SECONDS=$((ELAPSED % 60))

          echo ""
          echo "⏱️ Время сборки: ${MINUTES}м ${SECONDS}с"
          echo "✅ Сборка завершена!"

      # ============================================================================
      # ШАГ 8: Диагностика — почему модули не собрались
      # ============================================================================
      - name: Diagnose missing modules
        run: |
          echo "🔍 Диагностика отсутствующих модулей..."
          cd linux-build

          # Что говорит config про iwlwifi
          echo ""
          echo "📡 CONFIG_IWLWIFI в .config:"
          grep -E "^CONFIG_IWLWIFI|^CONFIG_IWLMVM" .config || echo "НЕ НАЙДЕНО в .config"

          # Что говорит config про cfg80211/mac80211
          echo ""
          echo "📡 CONFIG_CFG80211/MAC80211 в .config:"
          grep -E "^CONFIG_CFG80211=|^CONFIG_MAC80211=" .config || echo "НЕ НАЙДЕНО"

          # Поиск iwlwifi в дереве исходников
          echo ""
          echo "📁 iwlwifi в дереве исходников:"
          find drivers/net/wireless/intel -name "Makefile" -exec cat {} \; 2>/dev/null | head -20

          # Проверка что говорит make про iwlwifi
          echo ""
          echo "🔧 make silentoldconfig вывод для iwlwifi:"
          grep -r "iwlwifi\|iwlmvm" .config 2>/dev/null | head -10

          # Список всех собранных модулей
          echo ""
          echo "📋 Все собранные модули:"
          find . -name "*.ko" -o -name "*.ko.zst" 2>/dev/null | sort

          # Сохраняем финальный .config как артефакт
          cp .config "$GITHUB_WORKSPACE/minimal_config_after_build"

      # ============================================================================
      # ШАГ 9: Проверка результатов
      # ============================================================================
      - name: Check build results
        run: |
          echo "📊 Проверка результатов..."
          cd linux-build

          # Размер ядра
          echo ""
          echo "📦 Размер bzImage:"
          ls -lh arch/x86/boot/bzImage

          # Версия ядра
          echo ""
          echo "🏷️ Версия ядра:"
          cat include/config/kernel.release

          echo ""
          echo "✅ Ядро собрано успешно"

      # ============================================================================
      # ШАГ 10: Установка модулей
      # ============================================================================
      - name: Install modules
        run: |
          echo "📦 Установка модулей..."
          cd linux-build

          # Получаем версию ядра из .config
          KERNEL_RELEASE=$(cat include/config/kernel.release)
          echo "🏷️ Версия ядра: $KERNEL_RELEASE"

          # Устанавливаем модули
          mkdir -p modules_install/$KERNEL_RELEASE
          make INSTALL_MOD_PATH=modules_install/$KERNEL_RELEASE modules_install

          # Удаляем build/ (симлинк на исходники)
          echo ""
          echo "🗑️ Удаление build/ директории из модулей..."
          rm -rf modules_install/$KERNEL_RELEASE/build
          echo "✅ build/ удалён"

          echo "✅ Модули установлены"
          echo ""
          echo "📊 Размер модулей (после очистки):"
          du -sh modules_install/$KERNEL_RELEASE

          echo ""
          echo "📁 Все модули:"
          find modules_install/$KERNEL_RELEASE -name "*.ko*" | sort

      # ============================================================================
      # ШАГ 11: Загрузка артефактов
      # ============================================================================
      - name: Get kernel release version
        id: kernel_release
        run: |
          KERNEL_RELEASE=$(cat include/config/kernel.release)
          echo "kernel_release=$KERNEL_RELEASE" >> $GITHUB_OUTPUT
          echo "📊 Kernel release: $KERNEL_RELEASE"
        working-directory: linux-build

      - name: Upload kernel artifact
        uses: actions/upload-artifact@v4
        with:
          name: bzImage-${{ github.event.inputs.kernel_version || '6.19.10' }}
          path: linux-build/arch/x86/boot/bzImage
          retention-days: 30

      - name: Upload config artifact (original)
        uses: actions/upload-artifact@v4
        with:
          name: config-original-${{ github.event.inputs.kernel_version || '6.19.10' }}
          path: ${{ github.workspace }}/minimal_config
          retention-days: 30
          if-no-files-found: error

      - name: Upload config artifact (after olddefconfig)
        uses: actions/upload-artifact@v4
        with:
          name: config-after-olddefconfig
          path: ${{ github.workspace }}/minimal_config_final
          retention-days: 30
          if-no-files-found: error

      - name: Upload config artifact (after build)
        uses: actions/upload-artifact@v4
        with:
          name: config-after-build
          path: ${{ github.workspace }}/minimal_config_after_build
          retention-days: 30
          if-no-files-found: error

      - name: Upload modules build log
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            /tmp/modules_build.log
            /tmp/olddefconfig.log
            /tmp/prepare.log
          retention-days: 30
          if-no-files-found: warn

      - name: Upload modules artifact
        uses: actions/upload-artifact@v4
        with:
          name: modules-${{ steps.kernel_release.outputs.kernel_release }}
          path: |
            linux-build/modules_install/${{ steps.kernel_release.outputs.kernel_release }}/lib/modules/${{ steps.kernel_release.outputs.kernel_release }}/
            !linux-build/modules_install/${{ steps.kernel_release.outputs.kernel_release }}/lib/modules/${{ steps.kernel_release.outputs.kernel_release }}/build/**
          retention-days: 30
          if-no-files-found: error

      # ============================================================================
      # ШАГ 10: Финальное сообщение
      # ============================================================================
      - name: Build summary
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║              СБОРКА ЯДРА ЗАВЕРШЕНА! ✅                   ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "📦 Артефакты доступны для скачивания:"
          echo "   - bzImage (образ ядра)"
          echo "   - .config (конфигурация)"
          echo "   - modules (модули)"
          echo ""
          echo "⏱️ Для скачивания:"
          echo "   1. Вернитесь на страницу Actions"
          echo "   2. Откройте этот запуск"
          echo "   3. В разделе 'Artifacts' скачайте нужные файлы"
          echo ""
